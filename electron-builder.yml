appId: com.antoniolg.nextdj
productName: NextDJ
directories:
  buildResources: build
  output: release
files:
  - out/**
  - package.json
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
mac:
  icon: build/icon.icns
  target:
    - dmg
    - zip
  category: public.app-category.music
  identity: null
  extendInfo:
    NSCameraUsageDescription: NextDJ uses the camera for the picture-in-picture overlay in session recordings.
    NSMicrophoneUsageDescription: NextDJ requests audio access to list and route your audio output devices.
win:
  icon: build/icon.ico
  target:
    - nsis
    - portable
nsis:
  artifactName: ${productName}-${version}-${os}-${arch}-setup.${ext}
portable:
  artifactName: ${productName}-${version}-${os}-${arch}-portable.${ext}
linux:
  icon: build/icon.png
  category: Audio
  maintainer: Antonio Leiva <antonio@devexperto.com>
  syncDesktopName: true
  target:
    - AppImage
    - deb
npmRebuild: false
